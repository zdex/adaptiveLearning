package com.adap.learn.config;

import org.springframework.context.annotation.Configuration;

/**
 * ✅ OpenApiConfig
 *
 * Configures Swagger / OpenAPI documentation for your Spring Boot API.
 * This enables a web UI (Swagger UI) where you can test endpoints, provide
 * request bodies, and include JWT tokens for authentication.
 */
@Configuration
public class OpenApiConfig {
    // No code needed — annotations do all the setup.
}
