rootProject.name = 'adaptiveLearning'

include 'api'
include 'ui'

// Optional: set explicit project directories if structure differs
project(':api').projectDir = file('api')
project(':ui').projectDir = file('ui')
